# `cargv` tests
# =============

test_suite(
    name='test-all',
)

cc_test(
    name='test-cargv',
    timeout='short',
    srcs=glob(include=['*_test.cpp',],),
    deps=[
        '//:cargv',
        '@com_google_googletest//:gtest_main',
    ],
)
